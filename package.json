{
  "name": "hospital_management_website",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm ci --legacy-peer-deps && npm run build && cd ../server && npm ci --legacy-peer-deps",
    "postinstall": "npm run build"
  }
}
