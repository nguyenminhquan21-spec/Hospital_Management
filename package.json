{
  "name": "hospital_management_website",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm run build && cd ../server && npm install --legacy-peer-deps",
    "postinstall": "cd server && npm install --legacy-peer-deps && cd ../client && npm install --legacy-peer-deps"
  }
}
