{
  "name": "hospital_management_website",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "cd server && npm start",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build:client": "cd client && npm install --legacy-peer-deps && npm run build",
    "build:server": "cd server && npm install --legacy-peer-deps",
    "install:all": "npm install && cd client && npm install --legacy-peer-deps && cd ../server && npm install --legacy-peer-deps"
  }
}
